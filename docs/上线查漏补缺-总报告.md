# 上线前查漏补缺总报告（2026-08-25）

> 四路并行排查（portal / admin / api / 部署配置）的汇总。修复分工：
> **可解耦模块 → `docs/modules/` 接口契约文档**（AI 编码模型凭单篇文档即可实施）；
> **跨模块散点 → 主会话直接修复**（见 §5 修复记录）。

---

## §1 Portal 端发现（apps/portal）

### 上线必须修

| 问题                                                                                                                                                                                                             | 位置                                                               | 处置                                       |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | ------------------------------------------ |
| 用户中心假询盘是 useState 初始值，API 挂时真实用户看到虚构询盘+虚构销售跟进                                                                                                                                      | `components/dashboard/mock-inquiries.ts`、`DashboardClient.tsx:65` | 主会话已修                                 |
| 6 个虚构客户案例（虚构证言人/成果数字）在 API 空时当真实案例展示                                                                                                                                                 | `packages/shared/src/constants/cases/` + `lib/content.ts` 回退     | 待业主提供真实案例；回退逻辑保留（开发用） |
| ICP 备案号占位                                                                                                                                                                                                   | `messages/{en,zh}.json` `footer.icp`                               | 待业主提供真实备案号                       |
| 页脚 /terms、/sitemap、/cookie-policy 死链                                                                                                                                                                       | `components/layout/Footer.tsx:126-134`                             | 主会话已修（删死链）                       |
| about 页 i18n key 失配会渲染崩溃：Milestones 用 `m2016`（messages 只有 `m2017`）；GlobalPresence 默认 key `hq/frankfurt/singapore/houston`（messages 只有 shenzhen/suzhou/qingdao/overseas，且那三个海外点虚构） | `components/about/Milestones.tsx:5`、`GlobalPresence.tsx:7`        | 主会话已修                                 |
| Turnstile 未接入，询盘表单无防刷                                                                                                                                                                                 | `components/contact/ContactForm.tsx:192-195`                       | → 模块文档 05                              |

### 建议改进

- 硬编码数字 500+/30+/99.9%/50M+/200+/120+（HeroNarrative、StatsAndClients、RnDStrength）待业主核对，走 admin 站点设置
- UL 认证为示意版（Partners.tsx / Certifications.tsx），无证书须移除
- VideoShowcase 假播放按钮（点击无反应）；ProductViewer3D 型号按钮无功能（MBV20S/MFV30 不在产品注册表）
- `NEXT_PUBLIC_API_URL` 生产构建漏配会静默回退 mock → 模块文档 01 已含构建期注入
- 询盘跟进便签只写 localStorage（InquiryDrawer.tsx:60-72）；收藏删除静默失败（可接受）

### 已确认无问题

- AI 客服（SSE 流式真实）、登录/注册、询盘提交、收藏、用户资料——全部真实 API
- messages/en.json 与 zh.json 各 421 key 完全同步

## §2 Admin 端发现（apps/admin）

### 上线必须修

| 问题                                                                                                                         | 位置                                                                                        | 处置                                                      |
| ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| 仪表盘假待办 4 条 + 假浏览量 8,209                                                                                           | `dashboard/page.tsx:47-60,94-98`                                                            | → 模块文档 07-D                                           |
| 设置页基本信息/SMTP/通知设置 4 个假保存按钮                                                                                  | `settings/page.tsx:129-134,184-193,221-226`                                                 | 基本信息→主会话已修（settings KV）；SMTP/通知→模块文档 02 |
| AI 设置保存不生效（chat 不读配置）                                                                                           | `ai-settings/page.tsx` ↔ `api/chat`                                                         | → 模块文档 07-B                                           |
| content 页 Banner/文案/Footer 链接保存后 portal 不消费                                                                       | `content/page.tsx`                                                                          | → 模块文档 07-A（含下线决策）                             |
| 案例 Logo / 方案场景图 / 站点 Logo 三处死上传区域（无 input 无 handler，文案"占位，后端就绪后接 MinIO"已过时——MinIO 已就绪） | `CaseFormDialog.tsx:258-265`、`SolutionFormDialog.tsx:213-221`、`settings/page.tsx:110-115` | 主会话已修（接入素材位/上传 API）                         |
| 权限矩阵前后端不一致 + Sidebar 不按角色过滤                                                                                  | `staff/page.tsx:47-63`、`Sidebar.tsx:23-38`                                                 | → 模块文档 06                                             |
| Docker 构建未传 NEXT_PUBLIC_API_URL                                                                                          | `docker/admin.Dockerfile`、compose                                                          | → 模块文档 01                                             |

### 建议改进

- Topbar 只读搜索框/常亮通知红点/无效"个人信息"菜单 → 通知红点由模块 03 实装；其余主会话已清理
- lib/mock 三个文件数据半死/全死（类型仍在用）→ 主会话已清理数据、保留类型
- StubPage.tsx 死代码 → 主会话已删
- TrendChart 伪随机回退无标识 → 模块文档 07-D

## §3 后端发现（api/）

### 上线必须修

| 问题                                                     | 位置                                      | 处置                                                    |
| -------------------------------------------------------- | ----------------------------------------- | ------------------------------------------------------- |
| JWT_SECRET fallback `dev-secret-change-me`，compose 未传 | `auth.module.ts:12`、`jwt.strategy.ts:18` | → 模块文档 01-4（生产强制配置）                         |
| SMTP 未配时注册接口响应回传 devCode                      | `auth.service.ts:61-62`                   | → 模块文档 02-3（生产禁返）+ 主会话已加 production 兜底 |
| send-code 无限频                                         | `auth.controller.ts:11`                   | → 模块文档 02-3                                         |
| compose api 服务环境变量大面积缺失                       | docker-compose.yml                        | → 模块文档 01-2                                         |

### 桩/未实装（已知，文档化）

- RAG 检索管线：retrieval.service.ts 纯桩、chunks 死表、vectorStatus 永不推进 → 模块文档 04
- 邮件基础设施（询盘通知）→ 模块文档 02；后台通知系统 → 模块文档 03
- FAQ 有 CRUD 无消费方（04 完成后可让检索纳入 FAQ，或保持人工维护）
- 无定时任务/队列；Redis 在跑但代码零引用（01 文档建议下线或留给未来队列）
- User.aiUsageCount 只增不读（无用量限额）；EmailCode 表无过期清理（可接受）

## §4 部署/配置发现

详见模块文档 01。要点：NEXT_PUBLIC_* 构建期注入缺失（最致命）、根 `.env` 链路、HTTPS 未启用、双层 CORS 冲突、migrate/seed/init-minio 不在自动化路径、备份无轮转、内部端口外暴。

## §5 主会话散点修复记录（本报告对应提交）

- portal：DashboardClient 假询盘初始值→空态；删 `mock-inquiries.ts`；Milestones `m2016`→`m2017`；GlobalPresence 默认地点 key 对齐 messages；Footer 删 3 个死链；VideoShowcase 播放按钮无素材时禁用并提示
- admin：设置页基本信息保存接 settings KV；三处死上传区域接通 `/api/uploads/site-asset`（站点 Logo）与素材位说明（案例 Logo/方案场景图引导至素材管理）；Topbar 移除只读搜索框与无效菜单项；删 StubPage；lib/mock 数据清理（保留被引用类型）
- api：auth.service 生产环境禁返 devCode（02 模块实施前的安全兜底）

## §6 业主待办（任何 AI 无法代劳）

以 `docs/modules/README.md` 末尾清单为准（已按 2026-08-25 决策更新：ICP 删除、SMTP/RAG 暂缓、域名购买后启用 HTTPS）。

## §7 第二轮：业主决策执行（2026-08-25）

决策：纯海外发布（删 ICP）；SMTP 不实装（注册仅校验邮箱格式）；RAG 不实装（FAQ 驱动 AI 客服）；真实案例业主自行替换；域名购买中。

已实施：

- **portal**：Footer 删 ICP 占位与 `footer.icp` 文案键；注册表单移除验证码环节（schema/UI/messages 同步清理）；Footer 法律链接改由 `content-footer-links` 驱动
- **api**：auth 移除 send-code/verifyCode/EmailCode 写入与 nodemailer 内联代码；chat 注入 FAQ 上下文（60s 缓存）+ 读取 `ai-settings` 生效（模型映射/systemPrompt/限频/每日上限/降级文案）
- **admin**：设置页删 SMTP/通知 tab；AI 设置页对齐真实生效字段（删假 API Key/月度预算/GPT 选项）；content 页下线 Banner/多语言文案 tab（Footer 链接已接通 portal）
- **部署（模块 01 核心项）**：portal/admin Dockerfile 构建期 ARG `NEXT_PUBLIC_*`；compose api 环境补全（JWT_SECRET/DEEPSEEK/CORS/MINIO_*）+ migrate 一次性服务（迁移 + init-minio）+ api depends_on minio/migrate；nginx 删双层 CORS（统一交 Nest）+ 安全头 + 敏感端点限频；deploy.sh 加迁移说明/备份轮转/健康检查；`JWT_SECRET` 生产强制（`api/src/common/env.ts`）；seed 超管密码走 `SEED_ADMIN_PASSWORD`；.env.example 同步清理死变量；.gitignore 加 backups/
- 延期模块：02 邮件、04 RAG（文档保留，标记暂缓）
